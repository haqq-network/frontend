# shell-burn-waitlist

This library provides functionality for participating in burn waitlists.

## Running unit tests

Run `nx test shell-burn-waitlist` to execute the unit tests via [Jest](https://jestjs.io).

{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "deploymentEnabled": false
  },
  "buildCommand": "rm -rf .nx node_modules/.cache dist && pnpm nx build shell --prod --verbose --parallel=1"
}
